# Текст задания

## Модель потока управления для автоматизируемого бизнес-процесса

Модель процесса представлена в файле `docs/diagram (1).bpmn`. Она описывает взаимодействие консультанта, складской системы и клиента при выдаче заказа, включая проверку заказа, подготовку товара, выдачу или отказ с возвратом.

## UML-диаграммы классов и пакетов разработанного приложения

### UML-диаграмма пакетов (`mermaid`)

```mermaid
classDiagram
    direction LR

    class controller {
      <<package>>
    }
    class service {
      <<package>>
    }
    class db {
      <<package>>
    }
    class db_order {
      <<package>>
    }
    class domain {
      <<package>>
    }
    class domain_order {
      <<package>>
    }
    class domain_store {
      <<package>>
    }
    class dto_input {
      <<package>>
    }
    class dto_output {
      <<package>>
    }
    class exception {
      <<package>>
    }

    controller ..> service
    controller ..> dto_input
    controller ..> dto_output
    controller ..> exception

    service ..> db
    service ..> db_order
    service ..> domain
    service ..> domain_order
    service ..> domain_store
    service ..> exception

    db ..> domain
    db_order ..> domain_order

    dto_output ..> service
    dto_output ..> domain
    dto_output ..> domain_order
```

### UML-диаграмма классов (`mermaid`)

```mermaid
classDiagram
    direction LR

    class InventoryController
    class OrderController
    class PVZController
    class UserController
    class DeliveryPointController

    class StoreService
    class OrderService
    class StorageService
    class OrderStatusUpdater
    class UserRegistry
    class DeliveryPointRegistry

    class DtoMapper

    class ProductRepo
    class StoreRepo
    class OrderRepo
    class OrderItemRepo
    class UserRepo
    class DeliveryPointRepo

    class Product
    class StoreItem
    class Order
    class OrderItem
    class User
    class DeliveryPoint
    class Status

    InventoryController --> StoreService
    InventoryController --> DtoMapper

    OrderController --> OrderService
    OrderController --> DtoMapper

    PVZController --> StorageService
    UserController --> UserRegistry
    DeliveryPointController --> DeliveryPointRegistry

    StoreService --> ProductRepo
    StoreService --> StoreRepo

    OrderService --> ProductRepo
    OrderService --> StoreRepo
    OrderService --> OrderRepo
    OrderService --> OrderItemRepo

    StorageService --> OrderItemRepo
    StorageService --> OrderStatusUpdater

    OrderStatusUpdater --> OrderItemRepo
    OrderStatusUpdater --> OrderService

    UserRegistry --> UserRepo
    DeliveryPointRegistry --> DeliveryPointRepo

    ProductRepo --> Product
    StoreRepo --> StoreItem
    OrderRepo --> Order
    OrderItemRepo --> OrderItem
    UserRepo --> User
    DeliveryPointRepo --> DeliveryPoint

    Order --> Status
    OrderItem --> Order
    OrderItem --> Product
    StoreItem --> Product
```

## Спецификация REST API для всех публичных интерфейсов разработанного приложения

Базовый URL: `http://localhost:8080`

Инвентарь:
- `POST /inventory/products` - создать товар на складе. Тело: `ProductCreateRequestDto` (`name`, `price`, `initialCount`).
- `PUT /inventory/products/{id}` - обновить товар (имя/цена). Тело: `ProductUpdateRequestDto`.
- `PATCH /inventory/products/{id}/count?change=...` - изменить остаток товара.
- `GET /inventory/products/{id}` - получить товар с остатком.
- `GET /inventory/products` - список товаров с остатками.
- `GET /inventory/products/{id}/count` - получить количество товара на складе.

Заказы:
- `POST /order/create` - создать заказ. Тело: `OrderCreateRequestDTO`.
- `GET /order/{id}` - получить детали заказа.
- `PATCH /order/{id}/status?status=...` - изменить статус заказа.

ПВЗ:
- `POST /mark-delivered` - отметить доставку позиции заказа. Тело: `OrderItemDeliveredDto`.

Справочники:
- `POST /users` - создать пользователя. Тело: `UserCreateRequestDto` (`email`).
- `POST /delivery-points` - создать пункт выдачи. Тело: `DeliveryPointCreateRequestDto` (`name`, `address`).

## Исходный код системы или ссылка на репозиторий с исходным кодом

Исходный код расположен в каталоге: `C:\Users\admin\IdeaProjects\blss-java`.

## Выводы по работе

В ходе работы реализованы публичные API для управления товарами, заказами и обработкой выдачи. Сервисный слой покрывает основную бизнес-логику, контроллеры предоставляют REST-интерфейс. BPMN-модель синхронизирована с ключевыми действиями системы (проверка заказа, подготовка товара, выдача и обновление статусов), а UML-диаграммы отражают структуру приложения и зависимости между слоями.
