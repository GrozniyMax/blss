<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="fix-user-reference-20260404-01" author="m.s.taranenko">
        <preConditions>
            <tableExists tableName="orders"/>
        </preConditions>
        <sql>
            ALTER TABLE orders ALTER COLUMN owner TYPE TEXT;
            ALTER TABLE orders ALTER COLUMN owner SET NOT NULL;
            -- Drop FK constraint if it exists (may not exist if not created yet)
            ALTER TABLE orders DROP CONSTRAINT IF EXISTS orders_owner_fkey;
        </sql>
    </changeSet>

</databaseChangeLog>
