spring:
  application:
    name: blss
  jta:
    enabled: true
  datasource:
    jndi-name: java:jboss/PostgresDS
  liquibase:
    change-log: classpath:/db/changelog/changelog-master.xml

server:
  port: 8080

security:
  users:
    xml-path: ${SECURITY_USERS_XML_PATH:/opt/wildfly/standalone/configuration/users.xml}


