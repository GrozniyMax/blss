spring:
  application:
    name: blss
  jta:
    enabled: true
  datasource:
    jndi-name: java:jboss/PostgresDS
  liquibase:
    change-log: classpath:/db/changelog/changelog-master.xml

server:
  port: 25102

security:
  users:
    xml-path: ${SECURITY_USERS_XML_PATH:/wildfly-37.0.1.Final/standalone/configuration/users.xml}


